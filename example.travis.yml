notifications:
  email:
    on_success: never

matrix:
  include:
    - language: python
      python:
        - "3.7"
      install:
        - pip install -r requirements.txt
      script:
        - pytest
