notifications:
  email:
    on_success: never

matrix:
  include:
    # __CHANGE_ME_CIs_or_REMOVE_thisFile__
    - language: python
      python:
        - "3.7"
      install:
        - pip install -r requirements.txt
      script:
        - pytest
